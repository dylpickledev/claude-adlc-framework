# Claude ADLC Framework Environment Configuration
# Copy this file to .env and fill in your actual values
# See config/dbt-mcp-setup.md for detailed setup instructions

# =============================================================================
# dbt MCP Configuration - Choose Your Setup Method
# =============================================================================
# Option 1: Local MCP with .env Authentication (simplest - use these vars)
# Option 2: Local MCP with OAuth (secure - see dbt-mcp-setup.md)
# Option 3: Remote MCP with OAuth (team/cloud - see dbt-mcp-setup.md)

# --- Common Settings (All Options) ---
DBT_HOST=cloud.getdbt.com
DBT_PROD_ENV_ID=<your-production-environment-id>

# --- Option 1: API Token Authentication ---
# Uncomment and fill in for Option 1 (local + .env)
# DBT_TOKEN=<your-dbt-cloud-api-token>

# --- Option 2 & 3: OAuth Authentication ---
# Uncomment and fill in for Options 2 & 3 (OAuth)
# DBT_OAUTH_CLIENT_ID=<your-oauth-client-id>
# DBT_OAUTH_CLIENT_SECRET=<your-oauth-client-secret>

# --- Multi-cell Account Support ---
# Leave empty for single-cell accounts
MULTICELL_ACCOUNT_PREFIX=

# --- Local dbt Project Settings (Options 1 & 2 only) ---
DBT_PROJECT_DIR=~/claude-adlc-framework/repos/dbt_cloud
DBT_PATH=/usr/local/bin/dbt

# --- Feature Toggles ---
# Set to true to disable specific features
DISABLE_DBT_CLI=false           # Disable local dbt CLI operations
DISABLE_SEMANTIC_LAYER=false    # Disable semantic layer queries
DISABLE_DISCOVERY=false         # Disable dbt Cloud discovery API
DISABLE_SQL=true               # Disable direct SQL execution (recommended for safety)
DISABLE_REMOTE=false           # Disable remote dbt Cloud operations

