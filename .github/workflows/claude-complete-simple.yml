name: Claude Complete Simple

on:
  pull_request:
    types: [labeled]

jobs:
  complete-project:
    name: Simple Claude Completion
    runs-on: ubuntu-latest
    if: contains(github.event.label.name, 'claude:complete')

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Test completion
        run: |
          echo "🤖 Would complete project on branch: ${{ github.event.pull_request.head.ref }}"
          echo "Project detection and completion logic would go here"