{
  "mcpServers": {
    "dbt-local": {
      "command": "/usr/local/bin/uvx",
      "args": ["--env-file", ".env", "dbt-mcp"],
      "disabled": false,
      "autoApprove": [],
      "_comment": "Local MCP server with full dbt CLI, Admin API, and Fusion tools access"
    }
  }
}
